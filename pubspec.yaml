name: ethereum_codec
version: 2.0.0
publish_to: none

dependencies:
  convert: ^3.1.1
  pointycastle: ^3.6.2
  typed_data: ^1.3.1
  sprintf: ^7.0.0

  eth_abi_codec:
    git:
      url: https://github.com/nbltrust/dart-eth-abi-codec.git
      ref: 2.0.1

environment:
  sdk: ">=2.17.0 <3.0.0"
